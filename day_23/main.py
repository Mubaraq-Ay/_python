# virtual environments
# a virtual environment is an isolated python environment for one project with its own installed packages.

# it avoids conflicting of differnt projects.

# core commands

'''
    python -m venv venv        # creates a virtual environment named "venv" in your project folder
    source venv/bin/activate   # activates it (Linux/Fedora — you'll use this one)

    deactivate    -> leaves the virtual environment
'''